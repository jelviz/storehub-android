# V16.1.0

Hotfix for GitHub Actions Kotlin compilation failure in `LocalStore.assistantContext()`.

- Replaced ambiguous `sumOf { if (...) 3 else 0 }` with an explicit `count * 3` score.
- Keeps all V16 features unchanged.
- versionCode 17 / versionName 16.1.0

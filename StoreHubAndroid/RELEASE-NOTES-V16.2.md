# V16.2.0

ثبت هوشمند کالا دیگر روی مدل اشتباه OpenAI و حذف پس‌زمینهٔ وابسته به ML Kit گیر نمی‌کند.

- حذف پس‌زمینه سفید روی خود گوشی (flood-fill از لبه‌ها)؛ ML Kit فقط اگر آن کار نکرد
- عکس محصول WebP شفاف است (با آلفا)؛ برای تشخیص AI همان JPEG اصلی فرستاده می‌شود
- مدل پیش‌فرض AI: `gpt-4o-mini` / Gemini Flash — `gpt-5.6-sol` مدل Cursor بود و به API اوپن‌ای‌آی نمی‌خورد
- پشتیبانی Gemini و OpenRouter
- آپلود عکس با raw و multipart؛ اگر عکس نرود کالا باز هم ثبت می‌شود
- چرخش EXIF عکس دوربین
- versionCode 18 / versionName 16.2.0
